export interface MenuItemType {
  id: string;
  imageUrl: string;
  name: string;
  price: number;
}

export interface CartItemType {
  id: string;
  title: string;
  unitPrice: number;
  qty: number;
}
